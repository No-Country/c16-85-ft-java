spring:
  datasource:
    url: jdbc:mysql://localhost:3308/nocountry_db?serverTimezone=UTC
    username: dev
    password:  1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    properties:
      hibernate:
      format_sql: true